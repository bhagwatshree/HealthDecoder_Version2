{
  "name": "medical-scanner-backend",
  "version": "1.0.0",
  "description": "Backend for Medical Report Scanning Application",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "sam build",
    "deploy": "sam build && sam deploy",
    "deploy:guided": "sam build && sam deploy --guided",
    "gen-samconfig": "node scripts/gen-samconfig.js",
    "migrate": "node migrate.js"
  },
  "dependencies": {
    "@google/genai": "^2.9.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^14.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.11.5",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
